{
  "name": "@tspower/check",
  "version": "1.0.3",
  "description": "Simple Collection of checks functions",
  "main": "./lib/check.js",
  "types": "./lib/check.d.ts",
  "repository": {
    "url": "git+https://github.com/TsPowerOne/Check.git"
  },
  "directories": {
    "doc": "docs",
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "setup": "npm install",
    "build": "tsc",
    "build-watch": "tsc --watch",
    "test-only": "mocha --require ts-node/register test/**/*.ts",
    "pretest": "npm run build",
    "test": "npm run test-only",
    "typedoc": "typedoc --out ./docs --mode modules --tsconfig ./tsconfig.json "
  },
  "keywords": [
    "check",
    "functions"
  ],
  "author": "Luigi Senzamici (luigisenzamici@gmail.com)",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.1.6",
    "@types/mocha": "^5.2.5",
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "ts-node": "^7.0.1",
    "typedoc": "^0.15.0",
    "typescript": "^3.1.1"
  },
  "dependencies": {
    "@tspower/regexpr": "^1.0.2"
  },
  "bugs": {
    "url": "https://github.com/TsPowerOne/Check/issues"
  },
  "homepage": "https://tspowerone.github.io/Check/"
}
